<div class="row">
	<div class="col-md-9 content" role="main">
		<h2>Questões</h2>
		<table class="table table-style">
			<thead>
				<tr>
					<th>Título</th>
					<th>Disciplina</th>
					<th>Autor</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<% @questions.each do |q| %>
						<td><%= q.description %></td>
						<td><%= q.discipline.name %></td>
						<td><%= q.user.teacher.name %></td>
					<% end %>
				</tr>
			</tbody>
		</table>
		<%= link_to "Criar questão", rails_admin.new_path('question'), class: "btn btn-default ico-plus" %>
	</div>
</div>