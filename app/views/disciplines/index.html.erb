<h1>Listando disciplines</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Descrição</th>
      <th>Professor</th>
      <th>Curso</th>
      <th>Ações</th>
    </tr>
  </thead>

  <tbody>
    <% @disciplines.each do |discipline| %>
      <tr>
        <td><%= discipline.name %></td>
        <td><%= discipline.description %></td>
        <td><%= discipline.course.name %></td>
        <td nowrap>
          <%= link_to "", rails_admin.show_path(model_name: 'discipline', id: discipline.id), :class => "glyphicon glyphicon-eye-open", title: "Visualizar"  %>
          <%= link_to "", rails_admin.edit_path(model_name: 'discipline', id: discipline.id), :class => "glyphicon glyphicon-edit", title: "Editar" %>
          <%= link_to "", discipline_path(discipline), title: "Deletar", :method => :delete, :confirm => "Tem certeza que deseja remover essa disciplina?", :class => "glyphicon glyphicon-trash" %>
      </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Disciplina', rails_admin.new_path('discipline'), class: "btn btn-primary ico-plus" %>
