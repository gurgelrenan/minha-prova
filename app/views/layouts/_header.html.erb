<!-- Static navbar -->
    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>">Minha Prova</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <li><%= link_to "Professores", teachers_path %></li>
              <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                  <span class="ico-list"><%= "Ensino" %></span>
                  <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Universidades", colleges_path %></li>
                  <li><%= link_to "Cursos", courses_path %></li>
                  <li><%= link_to "Núcleos", cores_path %></li>  
                </ul>
              </li>
              <li><%= link_to "Disciplinas", disciplines_path %></li>
              <li><%= link_to "Questões", questions_path %></li>
              <li><%= link_to "Provas", tests_path %></li>
            <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <%= link_to "#{current_user.email}", teacher_path(current_user.teacher) %>
              <%= link_to "Logout", destroy_user_session_path, :method => :delete  %>
            <% else %>
              <li><%= link_to "Cadastre-se", new_user_registration_path %></li>
              <li><%= link_to "Login", new_user_session_path %></li>    
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
