<%= simple_form_for(@teacher, html: { role: "form"  }) do |f| %>
  <%= f.input :name %>
  <%= f.association :user, prompt: "Escolha um usuário" %>
  <%= f.association :core, prompt: "Escolha um núcleo" %>

  <%= f.button :submit, class: "btn btn-primary", value: "Cadastrar" %>
<% end %>
